<!DOCTYPE html>
<html data-theme="light">
  <%= render "layouts/head" %>
  <body class="flex">
    <%= render "layouts/header" if authenticated? %>
    <main class="w-full min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
