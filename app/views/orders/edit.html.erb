<div class="p-6">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-900">オーダー編集</h1>
    <%= link_to "戻る", order_path(@order), class: "btn btn-outline" %>
  </div>

  <%= render "form", order: @order, users: @users, button_text: "オーダーを更新" %>
</div>