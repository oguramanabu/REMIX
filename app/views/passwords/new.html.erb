<!-- Password Reset Modal -->
<div data-modal-target="modal" class="hidden fixed inset-0 z-50 bg-white/20" data-action="click->modal#closeBackground">
  <div data-modal-target="backdrop" class="absolute inset-0 flex items-center justify-center">
    <div class="relative bg-gray-100 rounded-lg shadow-xl max-w-md w-full mx-4">
      <div class="p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">パスワード再発行</h3>
        <%= render "passwords/new_form" %>
      </div>
    </div>
  </div>
</div>
